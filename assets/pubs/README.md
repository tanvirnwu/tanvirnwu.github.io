# Publication teaser images

Drop one figure per paper here, then reference it in `_data/publications.yml`:

```yaml
- id: specguard-iccv25
  thumbnail: /assets/pubs/specguard-iccv25.webp
```

Guidelines: ~600×450 (4:3), WebP preferred, keep under ~60 KB.
Cards without a `thumbnail:` field render text-only, unchanged.
